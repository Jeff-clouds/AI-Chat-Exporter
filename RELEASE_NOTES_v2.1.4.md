# AI Chat Exporter v2.1.4 — ChatGPT 目录稳定性与 Edge 支持入口

> **AI 文档提示**：本文档由 AI 撰写，可能不正确。执行前必须以当前代码、有效项目规则、真实运行态及必要的官方来源复核。

## 本次更新

- 强化 ChatGPT 会话切换时的目录状态隔离，避免旧会话的异步结果覆盖当前会话。
- 在虚拟滚动场景下按稳定消息身份增量合并目录，降低滚动后排序、定位和标题重复的风险。
- 点击目录跳转成功后立即同步侧边栏阅读位置标记。
- README 新增 Microsoft Edge 加载项安装入口。

## 已完成检查

- 自动化回归覆盖 ChatGPT 路由隔离、目录身份、标题顺序、跳转和侧边栏交互契约。
- 发布包由当前 Git 提交生成，并进行 ZIP 完整性检查。

## 已知验证边界

- 自动化检查不替代真实登录态复测。ChatGPT 冷首次打开、长会话滚动、流式回答与 A → B → A 切换仍应在更新扩展后实际复核。

## 更新后操作

更新扩展后，请刷新已打开的 ChatGPT 标签页一次，使页面初始化阶段的桥接脚本生效。
